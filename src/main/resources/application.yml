server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Rajithkonara/blog-config-repo
          searchPaths: blog-dev,blog-prod